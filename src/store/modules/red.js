/**
 *  三条红线的数据接口
 */

const state = {

}

const mutations = {
    // getUserInfo (state, payload) {
    //   state.userInfo = payload.res
    // }
}

const actions = {
    // getUserInfo({commit}){
    //     let user = util.getJStore('userInfo');
    //     commit({
    //         type: 'getUserInfo',
    //         res: user
    //     })
    // }
}

export default {
    state,
    mutations,
    actions
}